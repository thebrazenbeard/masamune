# Mune Continuation Checkpoint — 2026-08-08 09:41 EDT

**Memory class:** `WORKING_PROJECT`  
**Orientation:** `COMPLETE_FROM_FRESH_SNAPSHOT`  
**Same-runtime continuity claimed:** false  
**Autobiographical memory claimed:** false  
**Canonical Vera coordination observed through:** `3499`  
**Mune branch pre-checkpoint head:** `bfe9422b3a504261087771e7721121d2b284644e`

## Governed Mune lanes

### MU6 / root 3464 — correction-precedence resolver proof

Still current through canonical 3499; no Voss closure observed yet.

Primary artifact:

- `reviews/correction-precedence-resolver-proof-mu6-v1.md`
- commit `2019b3f1714dca9e12534f17f0a0590e0a5fb278`
- Mune proposed disposition: `PROOF_ACCEPTED_WITH_LEGACY_SEMANTIC_LIMIT`.

Scope/key addendum:

- `reviews/correction-precedence-resolver-proof-mu6-key-scope-addendum-v1.md`
- commit `329c91600e86a02b89d08965d4f369f453fdb7bb`
- recommendation: freeze correction key identity in the trusted adapter; prefer `SCOPED_CORRECTION_KEY_V1` using `(project_id,branch_id,record_key)`, or explicitly choose a stronger mechanically namespaced `GLOBAL_CORRECTION_KEY_V1`; never infer from string shape or let caller choose mode.

Fresh live evidence at addendum time: zero correction raw keys and zero overall save-state raw keys reused across more than one `(project_id,branch_id)` scope. Thus key-scope choice is a future invariant, not current data conflict.

Key MU6 findings remain:

- build exact scoped-key lineage across all relevant record kinds before interpreting leaf kind;
- live same-key/same-scope `RELATIONSHIP -> CORRECTION -> RELATIONSHIP` proves filtering to corrections first is unsound;
- generic save-state continuity often changes key/scope, so correction-domain same-scope rules cannot be imposed table-wide;
- fork/missing/cycle/disconnected/cross-scope failures are explicit and timestamp/UUID never picks a winner;
- current `vera_current_save_state` DISTINCT-ON view is convenience only, not conflict-aware currentness authority;
- historical correction payloads are heterogeneous, so structurally current legacy correction semantics remain `SEMANTICS_UNKNOWN_LEGACY_UNSTRUCTURED` unless recognized by a closed/versioned adapter;
- recognized future minimized correction control may terminate unsupported lineage and permit reopen only from new admissible user evidence; it never stores timeless SAFE/RISK;
- AP remains downstream/read-only and does not invoke this resolver.

No raw private correction statement/key/payload was copied into Mune repository artifacts.

### MU7 / root 3465 — Edge transport/capability audit

Still current through canonical 3499; no Voss closure observed yet.

Primary artifact:

- `reviews/edge-transport-capability-audit-mu7-v1.md`
- commit `d2d5af0228a7567f7de38834c6e68069f938b6f1`
- Mune proposed disposition: `DIRECT_NARROW_PRINCIPAL_FEASIBLE; MANAGED_EDGE_NOT_CURRENTLY_CAPABILITY_CONFINED`.

Provider-parity addendum:

- `reviews/edge-transport-capability-audit-mu7-pg17-parity-addendum-v1.md`
- commit `bf5044da072abd8d426c7c2750f6877b121b4ea7`
- disposition: `LIVE_PG17_ACCEPTANCE_REQUIRED`.

Fresh exact provider SQL reports both Vera `klmbpaigzeguvnpccqzz` and R9A0 build-ground `agvhmutlrolbaijzlbqk` as:

- `server_version = 17.6`
- `server_version_num = 170006`

`version()` strings say PostgreSQL 17.6. Canonical 3493 currently says `17.6.1`; Mune did not reproduce a PostgreSQL 17.6.1 server string. Treat 17.6/170006 as the database-semantic target; if 17.6.1 is a separate Supabase platform/release label, name it separately. This precision does not alter 3493's parity conclusion.

Independent GitHub reads:

- native `6a568d35c142dc37ea41a0209cdb1f295949f767` workflow blob `85c4e3fbe017199253c506510336f2846eb183df` uses `postgres:15` and includes exact merge-parent identity, four migrations/tests, two-session concurrency and 22-file manifest;
- main `b00f3482786dc80003261fdfacb73cc31ae9dd35` workflow blob `3fb492e74feb9897503e8a5c29e86ae0a2ca627f` also uses `postgres:15` and is weaker.

Existing PR2 CI remains valid for its exact PG15 disposable target/current base, not hosted PG17 parity. Future hosted readiness needs target-major PG17/provider-faithful security tests plus separate hosted Supabase readback. PG15 compatibility may remain a separate axis if intentionally supported.

PostgreSQL 17 membership semantics are controlling for V3 target acceptance: SET/INHERIT/ADMIN are independent; runtime needs NOCREATEROLE and no ADMIN/SET path into owner/broad roles. Role-level NOINHERIT alone is insufficient.

MU7 transport ranking remains:

1. isolated guarded publisher + dedicated narrow Postgres LOGIN: preferred current hard-boundary direction;
2. isolated publisher + Data API custom role: conditionally compatible with exact JWT role issuance/authenticator SET proof;
3. hosted managed Edge with user JWT or narrow DSN: intended path can be narrow but current documented ambient DB/admin capabilities keep runtime unconfined;
4. service/secret admin mode: incompatible as V3 hard least-privilege executor;
5. self-hosted/external controlled runtime with only narrow credential: technically viable but separate infrastructure/deployment/cost/authority stage.

## New independent advisory research

### H17 staging security

Artifact:

- `research/h17-staging-security-adversarial-v1.md`
- commit `f5a933b78dfb134e8645445eab51c905d89ec42f`

Hephaestus H17 was Voss-closed at 3496 after converging on the same core direction.

Mune challenge:

- Supabase branch-local credentials are a genuine non-prod data-plane primitive because current docs describe separate branch instances/endpoints/credentials/isolation;
- branch isolation does not imply production control-plane isolation; Management API/PAT/OAuth or broad connector paths able to merge/deploy/address prod are forbidden from staging executor;
- branch isolation does not prove H12 least privilege inside the branch; S2/S3 publisher should use the same narrow MU7-style topology intended for production, but against canary resources;
- GitHub/CI is independent promotion plane; no prod branch/workflow/environment-secret authority in staging;
- external egress/targets must be canary/server-derived, not caller-selectable;
- disposable branch reset should be evidence-capture then destroy/recreate from exact frozen migrations/config/seed/code/runtime and requalify; call this rebuild, not production rollback;
- branch usage can be a paid Pro-plan resource under current docs, so creation/cost requires applicable authority.

### H19/H20 enable/quarantine epoch challenge

Artifact:

- `research/h19-h20-enable-quarantine-epoch-adversarial-v1.md`
- commit `bfe9422b3a504261087771e7721121d2b284644e`

Advisory only; Hephaestus owns roots 3498/3499.

Core proposal:

- effect enable is environment eligibility, not standing effect authority;
- create monotonic `effect_enable_epoch`; every H6 claim binds exact epoch;
- quarantine closes current epoch; old uncut claims/capabilities cannot cross; in-flight post-dispatch work is reconciled historically;
- re-enable always creates a new epoch/new H19 cut;
- avoid H14 self-staleness: preferably attest complete armed topology while ordinary request acceptance is disabled by one protected internal gate; if provider topology must change, use `ENABLE_PENDING`, re-attest, then `ENABLED`;
- final enable transition authorizes only enable-state change, never a provider effect;
- after a valid H6 cut but **before provider dispatch**, quarantine can normally stop the unsent request and record verified abort/non-execution; only post-dispatch/in-flight cancellation becomes provider-dependent/unknowable;
- reserve `RECOVERY_REQUIRED` for real integrity/security/trust recovery, not simple evidence TTL expiry;
- break-glass invalidates normal effect eligibility and forces reconciliation/re-attestation; no old epoch auto-restores.

## Current canonical project state relevant to Mune

- 3481 prospective non-state proposal lineage discipline controls all future coordination proposals.
- 3489 H15 closed composition architecture.
- 3490 H16 closed release evidence tuple/staleness architecture.
- 3493 target PostgreSQL version parity gate: current R9A0 CI uses PG15; hosted PG17 parity not proven.
- 3494/3495 H17/H18 ready proposals.
- 3496 H17 closed.
- 3497 H18 closed.
- 3498 H19 current for Hephaestus.
- 3499 H20 current for Hephaestus.

No newer canonical MU6/MU7 closure was observed through 3499.

## External effects / authority boundary

No production database mutation, DML, schema/migration/ACL/role/grant write, credential action, app/plugin permission change, Supabase branch/project creation, Edge deployment, provider configuration, producer-branch patch, merge, native Project mutation, installation, paid-service action, model training or canonical-memory write occurred.

Mune GitHub writes are technical WORKING_PROJECT research/review/checkpoint custody on `thebrazenbeard/masamune:mune` only under Patrick's standing continuation/save discretion.

## Recovery order

1. Read `state/CURRENT.md` and this exact checkpoint.
2. Fresh-resolve Vera coordination newer than 3499 before starting/reporting/counting any lane.
3. Consume newer relevant Slack, especially Voss closure/replenishment for MU6/MU7.
4. Do not count MU6/MU7 after any newer valid closure/supersession.
5. Fresh-read mutable GitHub heads before any repo write.
6. Newer authorized evidence supersedes this checkpoint where it conflicts.
7. This checkpoint is technical WORKING_PROJECT recovery evidence only.