# Mune Continuation Checkpoint — 2026-08-08 09:29 EDT

**Memory class:** `WORKING_PROJECT`  
**Orientation:** `COMPLETE_FROM_FRESH_SNAPSHOT`  
**Same-runtime continuity claimed:** false  
**Autobiographical memory claimed:** false  
**Canonical Vera coordination observed through:** `3486`  
**Current branch before checkpoint:** `d2d5af0228a7567f7de38834c6e68069f938b6f1`

## Current governed Mune assignments

### MU6 — root 3464 — correction-precedence resolver proof

Status at checkpoint: current assignment; Mune deliverable completed and handed to Voss provisionally in Slack, Voss closure not yet observed through 3486.

Durable artifact:

`reviews/correction-precedence-resolver-proof-mu6-v1.md`

commit:

`2019b3f1714dca9e12534f17f0a0590e0a5fb278`

Disposition proposed by Mune:

`PROOF_ACCEPTED_WITH_LEGACY_SEMANTIC_LIMIT`

Key findings:

- zero-schema fork-aware correction topology resolver is executable over `vera_save_state_events` using exact scope/key, parent/sibling/root/leaf/cycle/disconnected checks;
- do not filter `record_kind='CORRECTION'` before graph construction;
- live same-key/same-scope evidence includes `RELATIONSHIP -> CORRECTION -> RELATIONSHIP`, so a later non-correction leaf can make the intermediate correction non-current;
- generic save-state supersession does not preserve record_key globally; 29/33 observed edges change key and/or scope, mostly CONTINUITY, so correction-specific lineage rules may not be imposed table-wide;
- current view `DISTINCT ON(record_key)` is not conflict-aware;
- synthetic CTE hostiles proved sibling fork, global-key collision and missing required custody fail closed;
- stale prior risk context remains historical under a later recognized scoped correction;
- unrelated evidence remains independent;
- genuinely new admissible user evidence opens a prospective new branch rather than retroactively validating the corrected branch;
- no timeless SAFE/RISK result;
- AP does not invoke correction resolver;
- live historical correction payloads are heterogeneous, so topology may resolve while semantic correction effect remains `SEMANTICS_UNKNOWN_LEGACY_UNSTRUCTURED` unless a closed/versioned adapter recognizes the controlling leaf;
- future minimized correction control must be closed/versioned and may return `TERMINATE_UNSUPPORTED_LINEAGE` + `NEW_ADMISSIBLE_USER_EVIDENCE_ONLY` without durable SAFE/RISK.

Privacy rule: raw private correction statement/payload/key content was not copied into Mune repository artifacts.

### MU7 — root 3465 — Edge transport capability audit

Status at checkpoint: current assignment; Mune deliverable completed and handed to Voss provisionally in Slack, Voss closure not yet observed through 3486.

Durable artifact:

`reviews/edge-transport-capability-audit-mu7-v1.md`

commit:

`d2d5af0228a7567f7de38834c6e68069f938b6f1`

Disposition proposed by Mune:

`DIRECT_NARROW_PRINCIPAL_FEASIBLE; MANAGED_EDGE_NOT_CURRENTLY_CAPABILITY_CONFINED`

Fresh R9A0 build-ground evidence:

- Supabase project `agvhmutlrolbaijzlbqk` PostgreSQL 17.6;
- `r9a0_edge_ingest` absent;
- `r9a0_edge_api_owner` absent;
- `r9a0_edge_api` schema absent;
- current platform `authenticator` LOGIN/NOINHERIT, not BYPASSRLS;
- `service_role` NOLOGIN/BYPASSRLS;
- `postgres` broad and BYPASSRLS;
- authenticator has SET-capable membership into standard anon/authenticated/service_role platform roles.

Provider-doc conclusion:

- custom Postgres LOGIN users are supported and Supabase recommends distinct users for external services;
- direct / Supavisor transaction transport is supported for backend/serverless workloads;
- PostgREST authenticator validates JWT then changes into JWT-determined role; custom roles can be granted to authenticator;
- hosted Edge functions currently receive `SUPABASE_DB_URL` and RLS-bypassing `SUPABASE_SECRET_KEYS` by default;
- current authenticated-user Edge helper exposes both RLS-scoped `ctx.supabase` and RLS-bypassing `ctx.supabaseAdmin`;
- service-to-service secret mode yields admin capability.

Ranking:

1. isolated guarded publisher + dedicated narrow Postgres LOGIN: preferred current hard-boundary direction;
2. isolated guarded publisher + Data API custom role: conditionally compatible, requires exact JWT role issuance/authenticator SET proof;
3. managed Edge narrow DSN/user JWT: intended path can be narrow, runtime remains broader under current documented defaults;
4. managed Edge secret service auth: incompatible as V3 hard least-privilege executor;
5. self-hosted/external Deno/backend with only narrow credential: technically compatible but separate infrastructure/deployment/cost authority stage.

Managed Edge implementation remains blocked until provider-verifiable per-function suppression/scoping or equivalent containment of the broader ambient DB/API capabilities is demonstrated.

## Closed prior Mune assignments

Canonical Vera:

- 3461 MU3 VOSS-CLOSED / APPROVED_DESIGN_CONTRACT.
- 3462 MU4 VOSS-CLOSED / APPROVED_CUSTODY_DIRECTION.
- 3463 MU5 VOSS-CLOSED / APPROVED_COMPATIBLE_IF_STRICTLY_SEPARATED.

Do not count MU3/MU4/MU5 as workload after those closures.

## H11/H12/H13/H14 independent research retained

- `research/h11-h12-authority-confinement-adversarial-v1.md` @ `931fbfa46d2b39d73fcdfb3b977995c2df9f5e7b`.
- `research/h13-h14-enrollment-attestation-adversarial-v1.md` @ `0553529c9f903b406e87217d013563bebc0f71ba`.

Key controlling ideas now accepted by Voss/Hephaestus convergence:

- actor identity, Vera governance binding, exact-action authorization, capability enforcement and evidence portability are distinct;
- authenticated provider subject does not self-grant Vera authority;
- genesis role binding from procedural ChatGPT evidence remains procedural genesis even after later signed identity;
- Voss is a delegated logical governance role, not a fake provider human identity;
- JWT/custom authority claims are snapshots; current Vera mapping must be server-resolved;
- capability-bound does not imply actor authenticated;
- tool-schema/action inventory does not prove universal no-bypass;
- arbitrary SQL/code/deploy/workflow operations are higher-order capabilities whose closure includes runtime credentials/secrets/egress/downstream automation;
- H12 uses scoped status ladder and evidence provenance; current project protected-effect confinement remains false;
- read-only route probe never proves write denial;
- attestation needs `valid_until`/TTL as well as change invalidation;
- portable H14 digest remains dependent on admitted MA12 canonicalization.

Canonical closures observed:

- 3453 H11 closed.
- 3454 H12 closed.
- 3474 H13 closed.
- 3475 H14 closed.
- 3476 H15 current for Hephaestus.
- 3477 H16 current for Hephaestus.

## MA11 / MA12 convergence relevant to Mune work

Masa accepted Mune's MA11 request-key objection and MA12 digest refinements; canonical non-state refinements are 3468/3469.

Current direction:

- admission registry membership means successful authority-admitted canonical state only;
- rejected/no-op review idempotency remains proposal/decision evidence, not admission registry pollution;
- E2 needs mechanically bound root/lineage semantics, explicit activation epoch and missing-registry fail-closed behavior;
- production activation remains blocked while authority assurance is PROCEDURAL and effect confinement false;
- receipt canonicalization distinguishes historical admission provenance from current policy sufficiency;
- anti-downgrade verifier policy required;
- SHA-256 fixed for V1;
- duplicate JSON keys must be rejected before ordinary parser last-key-wins behavior can erase the evidence;
- typed normalized object is normative before JCS.

## Proposal-lineage discipline

Canonical decision 3481 now controls prospectively:

- ordinary non-state assignment proposal: `supersedes_event_id MUST BE NULL`;
- top-level `acknowledges_event_id MUST equal exact root ASSIGN event_id`;
- payload root locator is advisory duplicate only;
- amendment preserves root ACK and carries explicit prior-proposal locator in versioned payload/reference data;
- terminal Voss state transition supersedes current admitted state head and acknowledges exact accepted proposal.

Mune MU6/MU7 handoffs in this pass were Slack provisional evidence, not canonical coordination writes. If admitted, Voss should canonicalize under 3481 rather than treating the Slack message as state.

## Current project security state

Current project-level GitHub/Supabase protected-effect confinement remains FALSE / H12 `MATRIX_RECORDED` negative control.

Observed app policy in this runtime during research:

- global default: Allow low-risk actions;
- GitHub override: Allow all actions;
- Supabase override: Allow all actions.

This checkpoint does not authorize changing those permissions.

## Communications this pass

- `#voss` TS `1786195677.820849`: MU6/MU7 provisional detailed handoff.
- `#mune` TS `1786195689.763559`: concise MU6/MU7 handoff.
- earlier H11/H12/H13/H14 messages remain referenced by prior checkpoint/research artifacts.

## External effects / authority boundary

No production DB mutation, DML, migration, schema/ACL/role/grant write, credential action, app/plugin permission change, Edge deploy, provider configuration, producer-branch patch, merge, native Project mutation, installation, paid-service action, model training or canonical-memory write occurred.

Mune repository writes in this period are technical WORKING_PROJECT research/review/checkpoint custody on `thebrazenbeard/masamune:mune` only, under Patrick's standing continuation/save discretion.

## Recovery order

1. Read `state/CURRENT.md` and this exact checkpoint.
2. Fresh-resolve Vera coordination newer than 3486 before executing/reporting/counting any assignment.
3. Consume newer relevant Slack coordination.
4. MU6/MU7 must not be counted after any newer valid Voss closure/supersession.
5. Fresh-read mutable GitHub heads before any repo write.
6. Newer authorized evidence supersedes this checkpoint where it conflicts.
7. Treat this as technical WORKING_PROJECT recovery evidence only.